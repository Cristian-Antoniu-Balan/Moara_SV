const ListBar = ({ wide, opp }) => {
    return (
        <div
            className="list-bar"
            style={{width: `${wide}%`, opacity: `${opp}%`}}>
        </div>
    )
}

export default ListBar;